==========
TODO
==========

*Fix fill(order) in orderbook.py, as it shares responsibilities with add_fill(fill)
*Finish documentation
*Write tests, especially for order flow
*Split algorithms into separate project
*Investigate using Decimal, or ints with a multiplier to avoid floating point issues
